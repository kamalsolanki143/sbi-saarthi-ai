# ============================================================
# SAARTHI AI — Backend Dependencies
# FastAPI + LangGraph + Gemini + ChromaDB + Redis
# ============================================================

# --- Core Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9          # file uploads (voice, OCR docs)
python-jose[cryptography]==3.3.0  # JWT session tokens

# --- Pydantic v2 (guardrails + validation) ---
pydantic==2.7.1
pydantic-settings==2.2.1

# --- LangGraph + LangChain (orchestration) ---
langgraph==0.1.5
langchain==0.2.1
langchain-google-genai>=1.0.3
langchain-community==0.2.1

# --- Google Gemini ---
google-generativeai>=0.7.0

# --- Redis (session/short-term memory) ---
redis==5.0.4
hiredis==2.3.2                   # C parser for speed

# --- ChromaDB (vector/long-term memory) ---
chromadb==0.5.0

# --- HTTP client (external APIs) ---
httpx==0.27.0
aiohttp==3.9.5

# --- OCR ---
pytesseract==0.3.10
Pillow==10.3.0
pdf2image==1.17.0

# --- Environment & Config ---
python-dotenv==1.0.1

# --- Utilities ---
numpy==1.26.4
python-dateutil==2.9.0
pytz==2024.1

# --- Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                    # async test client
pytest-cov==5.0.0

# --- Sarvam AI (Indian language voice: STT/TTS/translation) ---
sarvamai>=0.1.28

# --- Logging ---
structlog==24.1.0
